name: WordPress Pattern Validation
permissions:
  contents: read
on: [push, pull_request, workflow_dispatch]

jobs:
  pattern-validation:
    uses: bcgov/des-git-workflows/.github/workflows/pattern-validate.yml@bug/DESCW-3169_exit-pattern-validate-early